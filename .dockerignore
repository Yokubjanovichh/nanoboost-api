# Secrets and local env (CRITICAL — must never enter the image)
.env
.env.local
.env.*.local
.env.production
.env.staging
.env.development

# VCS & CI metadata
.git
.gitignore
.github

# Python build / cache artifacts
__pycache__
*.py[cod]
*$py.class
*.so
.Python
.venv
venv
env
ENV
*.egg-info
*.egg
build
dist

# Tests are not needed at runtime
tests
.pytest_cache
.coverage
.coverage.*
htmlcov
coverage.xml
.hypothesis
.tox

# Lint / type-checker caches
.ruff_cache
.mypy_cache
.pyre
.pyright
.ty

# Docs (rebuild from source if you need them in the image)
docs

# Local Docker volume mount targets
uploads
postgres_data

# Legacy migration payloads
services-data.json

# IDE / OS noise
.vscode
.idea
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Claude / AI assistants
.claude
.cursor
